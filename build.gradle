subprojects{
    apply plugin: 'java'
    apply from: "${rootDir}/lint.gradle"

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}